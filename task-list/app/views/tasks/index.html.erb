<table class = "all-tasks">

	<tr>
		<th>Task</th>
		<th class="center">Edit</th>
	</tr>

	<% @all_tasks.each do |task| %>
	  <tr>
      <td><%= link_to task.title, task_path(task.id) %></td>
			<td class="center">
				<%= button_to "Delete", task_path(task.id),
				method: :delete,
				data: { confirm: "Do you want to delete this forever?" } %>
			</td>
    </tr>
	<% end %>

</table>
