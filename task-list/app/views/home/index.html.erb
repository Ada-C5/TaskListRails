<table>
	<tr>
		<th>Task Name</th>
		<th>Description</th>
		<th>Completed At</th>
	</tr>
	<% @all_tasks.each do |task| %>
	  <tr>
      <td><%= link_to task.title, "/#{task.title}" %></td>
      <td><%= task.description %></td>
      <td><%= task.completed_at %></td>
    </tr>
	<% end %>
</table>
