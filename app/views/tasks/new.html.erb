<hi> Add your new task below!</h1>

  <pre><%= params %></pre>

<%= form_for @task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :Person %>
  <%= f.select "person_id", options_from_collection_for_select( @people , "id", "name", :include_blank => true) %><br>

  <%= f.submit %>
<% end %>
