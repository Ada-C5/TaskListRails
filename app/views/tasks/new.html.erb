<pre><%= params %><pre>
<h1> Let's make a new task</h1>

<%= form_for @task do |f| %>
	<%= f.label :name %>
  <%= f.text_field :name %>
  	<%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.label :owner %>
  <%= f.collection_select :person_id, Person.all, :id, :name %>
  <%= f.submit %>
<% end %>
